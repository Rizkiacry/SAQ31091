#include <iostream>
#include <algorithm>
//#include <balls>

int main() {
	using namespace std;
	
	for (int i = 0; i < ; ++i) {
		cin >> num[i];
	}
	
	int min1 = min(num[5]);
	int max2 = max(num[5]);
	
	cout << "min" << min1;
	cout << "max" << max2;
}
